/**
 * Creating a sidebar enables you to:
 - create an ordered group of docs
 - render a sidebar for each doc of that group
 - provide next/previous navigation

 The sidebars can be generated from the filesystem, or explicitly defined here.

 Create as many sidebars as you want.
 */

// @ts-check

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  // By default, Docusaurus generates a sidebar from the docs folder structure
  hydroSidebar: [
    {
      type: 'html',
      value: '<h1>Learn Hydro</h1>', // The HTML to be rendered
      defaultStyle: true, // Use the default menu item styling
    },
    {type: 'autogenerated', dirName: 'hydro/learn'},
    {
      type: 'html',
      value: '<h1 class="with-border"><a href="/rustdoc/hydro_lang/">Rustdoc <svg width="12" height="12" aria-label="(opens in new tab)" style="margin-left: 0.3rem"><use href="#theme-svg-external-link"></use></svg></a></h1>',
      defaultStyle: true,
    },
    {
      type: 'html',
      value: '<h1 class="with-border">Core Framework</h1>', // The HTML to be rendered
      defaultStyle: true, // Use the default menu item styling
    },
    {type: 'autogenerated', dirName: 'hydro/reference'},
  ],
  dfirSidebar: [{type: 'autogenerated', dirName: 'dfir'}],
};

module.exports = sidebars;
